[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "cd apps/agents && pip install -r requirements.txt"
]

[start]
cmd = "cd apps/agents && uvicorn main:app --host 0.0.0.0 --port $PORT"
